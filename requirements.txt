flask==3.1.3
gunicorn==23.0.0
composio-core>=0.6.0,<1.0
requests>=2.31
anthropic>=0.40,<1.0
# Pillow temporarily disabled — agent rendering tools degrade gracefully.
# Restore once we identify what's failing the Render free-tier build.
